# include <stdio.h>
void main()
{
    float length;
    float finalAns;
    printf("enter the length of square  ");
    scanf("%f",&length);
    printf("%f\n",length);
    finalAns=length*length;
    printf("the arrea of square is %f",finalAns);   
}